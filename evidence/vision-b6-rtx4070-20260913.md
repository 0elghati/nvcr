# B6 P-frame orchestration evidence — RTX 4070 — 2026-09-13

## Scope and identity

- B5 code baseline: `5c2b5db35015f5ff7e45a1ae7dca20150e6e028e`
- B6 code commit: `0f4f3ae6243101f6fe2837208e0a34fba9efd077`
- Branch: `codex/b6-p-frame-orchestration`, stacked on the B5 branch
- GPU: NVIDIA GeForce RTX 4070, compute capability 8.9
- Driver: 580.173.02
- CUDA compiler: 12.8.93
- TensorRT: 10.9.0
- Model set: `dcvcrt-cvpr2025`
- Exact engine profiles: QCIF, CIF, 360p, 540p, 720p, and 1080p from
  `build/engines-b3-rtx4070-catalog`

The codec-side `PredictedOrchestration` owns the named P-frame stage set,
effective QP and reference selection, P-frame CDF and quantization assets, rANS
state, and NVP1 parsing and assembly. Codec-side `ReferenceState` owns feature
availability, reference generation/index matching, commit, and reset meaning.
TensorRT retains the provider-backed device tensors, CUDA transforms, stage
submission, transfers, synchronization, graph caching, and profiling.

The old TensorRT-local P-frame asset loader, raw engine-index invocation wrapper,
QP helper, and stale DPB struct were deleted. No parallel compatibility path
remains.

## Clean build and lifecycle gates

CPU Release configuration:

```bash
rtk cmake -S . -B /tmp/nvcr-b6-78864cd-cpu \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_INSTALL_PREFIX=/tmp/nvcr-b6-78864cd-install \
  -DNVCR_ENABLE_TENSORRT=OFF \
  -DNVCR_FETCH_DEPENDENCIES=OFF \
  -DNVCR_BUILD_TESTS=ON
```

- CPU Release build: pass
- CPU CTest: 18/18 pass
- CPU install: pass

The Clang Debug sanitizer tree used TensorRT disabled,
`NVCR_ENABLE_SANITIZERS=ON`, `NVCR_ENABLE_FUZZING=ON`, tests enabled, and
the CLI/examples disabled.

- Sanitizer build: pass
- Sanitizer CTest: 15/15 pass
- `rtk /tmp/nvcr-b6-78864cd-sanitized/tests/nvcr_access_unit_fuzz
  -runs=1000`: 1,000 runs pass under ASan/UBSan

The clean TensorRT Release tree reused the exact B4/B5 six-profile command,
model checkout, golden input, and engine paths with build directory
`/tmp/nvcr-b6-78864cd-trt`.

- TensorRT Release build: pass
- Non-GPU CTest: 19/19 pass
- GPU CTest: 13/13 pass

The GPU selection covers engine contracts and I/P round trips for all six exact
profiles plus `nvcr_dcvcrt_i_frame_golden`. Each round-trip test covers two
GOPs, I/P classification, dimensions, timestamps, reset, reuse, and decoded
output. The contract suite covers flush/drain and reset/reuse. Malformed and
bounded input coverage remains in the parser, format, payload, rANS, artifact,
and engine-contract tests.

The new dependency-free `nvcr_dcvcrt_orchestration` test covers missing
reference state, first-P frame selection, feature reuse, the 64-frame
frame-reference refresh, QP bounds, generation mismatch, and reset.

## Byte and reconstruction parity

The B6 binary encoded and decoded the same first 65 frames of
`/home/oelghati/datasets/qcif/akiyo_qcif.yuv` at 176x144, QP 32, GOP 8, and
30 fps through the retained QCIF bundle. This covers nine I frames, 56 P frames,
and multiple GOP resets.

Both B5/B6 `cmp -s` comparisons passed. Complete `.nvcr` stream SHA-256:

`9896684474818032506d32ac7d065a7710d9c26197a9d374f275d2b65f83f81a`

Decoded YUV SHA-256:

`10e02e460ec4477e9a7e41f72bbb44c5222e0f6773bf581cf874f74b97363008`

Each encoder reported 14,669 NVAU payload bytes. NVAU, NVI1, NVP1, rANS,
model, QP, input, target, and engine identities did not change.

## Performance and provider profile

Matched B6 packages:

- `/tmp/nvcr-b6-normal-profile-0f4f3ae-r1`
- `/tmp/nvcr-b6-gop1-clean-0f4f3ae`

Both use the same five exact profiles, full local sequences, QP 32, Release
settings, one warm-up, and three clean measured runs as B5. The normal-I/P
package has one separate profiled run. Both identify the clean B6 code commit.

| Mode | Metric | B5 | B6 | Delta |
|---|---:|---:|---:|---:|
| Normal I/P | Encode FPS | 113.741796 | 113.793460 | +0.045% |
| Normal I/P | Decode FPS | 120.378948 | 120.186062 | -0.160% |
| All-intra | Encode FPS | 42.466974 | 42.458823 | -0.019% |
| All-intra | Decode FPS | 46.987801 | 46.818796 | -0.360% |

Raw B6 clean samples:

| Mode/profile | Encode FPS runs | Decode FPS runs |
|---|---|---|
| I/P QCIF | `977.030, 976.034, 976.353` | `1058.731, 1062.078, 1057.785` |
| I/P CIF | `596.119, 595.713, 596.227` | `604.620, 605.174, 604.749` |
| I/P 360p | `418.733, 418.778, 418.610` | `395.392, 395.222, 395.712` |
| I/P 720p | `96.835, 96.745, 96.778` | `103.512, 102.950, 103.557` |
| I/P 1080p | `44.904, 44.951, 44.943` | `47.795, 47.852, 47.840` |
| Intra QCIF | `520.937, 520.701, 520.891` | `586.801, 587.729, 587.111` |
| Intra CIF | `262.092, 262.027, 262.592` | `276.424, 274.438, 276.444` |
| Intra 360p | `158.088, 157.844, 158.076` | `158.119, 158.387, 157.887` |
| Intra 720p | `37.665, 37.670, 37.660` | `42.127, 42.191, 42.055` |
| Intra 1080p | `16.125, 16.138, 16.128` | `17.857, 17.892, 17.855` |

All five matched normal-I/P payload records, PSNR values, input/model/target and
engine identities, and provider-profile records are exactly equal between B5
and B6. The provider comparison includes allocation, H2D, D2H, D2D,
synchronization, context-policy, and CUDA graph fields. Peak GPU memory remains
2,244 MiB. The worst matched per-profile throughput change is -0.400% for
1080p all-intra decode; the worst normal-I/P change is -0.220% for 720p decode.

The benchmark packages are marked partial only because the Python reference,
source-build, and core/GPU-test rows were intentionally not duplicated inside
these regression packages. The clean build/test gates above and the pinned
Python/native golden passed separately.

## Remaining decision

All B6 technical gates pass on the available RTX 4070. No repository document
contains an approved numeric performance limit, so B6 remains open until these
measurements are explicitly accepted or a limit is approved and applied.
