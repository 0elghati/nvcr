# Vision B5 I-frame orchestration evidence

Recorded on 2026-09-13 for the DCVC-RT I-frame ownership extraction.

B5's technical gates pass on the available RTX 4070. B5 remains open because
the repository has no approved numeric performance acceptance limit.

## Identity and ownership

- Base source: `790d74847743be8da1d88e521d8ffe6939307666`
- Measured B5 source: `5c2b5db35015f5ff7e45a1ae7dca20150e6e028e`
- Branch: `codex/b5-i-frame-orchestration`
- GPU: NVIDIA GeForce RTX 4070, compute capability 8.9
- Driver: 580.173.02
- CUDA compiler: 12.8.93
- TensorRT: 10.9.0
- Exact engine profiles: QCIF, CIF, 360p, 540p, 720p, and 1080p from
  `build/engines-b3-rtx4070-catalog`

`src/dcvcrt/orchestration.cpp` owns the I-frame CDF and quantization assets,
rANS state, NVI1 payload parsing and assembly, and the named I-frame stage set.
`src/dcvcrt/backend/tensorrt/backend.cpp` executes those stage handles and owns
device allocation, CUDA transforms, copies, synchronization, graph caching,
and profiling.

## Clean build and test gates

CPU Release configuration:

```bash
rtk cmake -S . -B /tmp/nvcr-b5-790d748-cpu \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_INSTALL_PREFIX=/tmp/nvcr-b5-790d748-install \
  -DNVCR_ENABLE_TENSORRT=OFF \
  -DNVCR_FETCH_DEPENDENCIES=OFF \
  -DNVCR_BUILD_TESTS=ON
```

- `rtk cmake --build /tmp/nvcr-b5-790d748-cpu --parallel`: pass
- `rtk ctest --test-dir /tmp/nvcr-b5-790d748-cpu --output-on-failure`:
  17/17 pass
- `rtk cmake --install /tmp/nvcr-b5-790d748-cpu`: pass

Sanitizer configuration used Clang Debug with TensorRT disabled,
`NVCR_ENABLE_SANITIZERS=ON`, `NVCR_ENABLE_FUZZING=ON`, tests enabled, and the
CLI/examples disabled.

- Sanitizer build: pass
- Sanitizer CTest: 14/14 pass
- `rtk /tmp/nvcr-b5-790d748-sanitized/tests/nvcr_access_unit_fuzz \
  -runs=1000 -timeout=5 -max_total_time=10`: 1,000 runs pass under ASan/UBSan

The TensorRT Release configuration reused the B4 command and exact bundle
paths recorded in `vision-b4-closure-rtx4070-20260913.md`.

- TensorRT Release build: pass
- `rtk ctest --test-dir /tmp/nvcr-b5-790d748-trt --output-on-failure -LE gpu`:
  18/18 pass
- `rtk ctest --test-dir /tmp/nvcr-b5-790d748-trt --output-on-failure -L gpu`:
  13/13 pass

The GPU selection covers engine contracts and I/P round trips for all six exact
profiles plus `nvcr_dcvcrt_i_frame_golden`.

## Byte and reconstruction parity

The B5 binary encoded and decoded the same first 65 frames of
`/home/oelghati/datasets/qcif/akiyo_qcif.yuv` at 176x144, QP 32, GOP 8, and 30
fps through the exact QCIF bundle used for B4. This covers nine I frames, 56 P
frames, and multiple GOP resets.

Both B4/B5 `cmp -s` comparisons passed. The complete `.nvcr` streams have
SHA-256:

`9896684474818032506d32ac7d065a7710d9c26197a9d374f275d2b65f83f81a`

The decoded YUV files have SHA-256:

`10e02e460ec4477e9a7e41f72bbb44c5222e0f6773bf581cf874f74b97363008`

Each encoder reported 14,669 NVAU payload bytes. NVAU, NVI1, rANS, model, QP,
and engine-manifest identity did not change.

## Performance and provider profile

The matched B5 packages are:

- `/tmp/nvcr-b5-normal-profile-5c2b5db`
- `/tmp/nvcr-b5-gop1-clean-5c2b5db`

Both use the same five exact profiles, QP 32 inputs, Release settings, one
warm-up, and three clean measured runs as B4. The normal-I/P package has a
separate profiling run. Both identify the clean B5 commit.

| Mode | Metric | B4 | B5 | Delta |
|---|---:|---:|---:|---:|
| Normal I/P | Encode FPS | 113.691319 | 113.741834 | +0.044% |
| Normal I/P | Decode FPS | 119.942644 | 120.379100 | +0.364% |
| All-intra | Encode FPS | 42.118595 | 42.466987 | +0.827% |
| All-intra | Decode FPS | 46.325395 | 46.987827 | +1.430% |

Raw B5 clean samples:

| Mode/profile | Encode FPS runs | Decode FPS runs |
|---|---|---|
| I/P QCIF | `976.481, 977.254, 976.312` | `1061.761, 1062.313, 1061.346` |
| I/P CIF | `596.567, 597.094, 596.659` | `602.273, 604.784, 603.715` |
| I/P 360p | `418.618, 418.992, 418.935` | `395.473, 394.246, 394.480` |
| I/P 720p | `96.822, 96.778, 96.682` | `103.607, 103.619, 103.474` |
| I/P 1080p | `44.891, 44.936, 44.871` | `47.963, 47.831, 47.958` |
| Intra QCIF | `521.047, 521.506, 521.194` | `584.735, 587.264, 586.958` |
| Intra CIF | `262.346, 262.289, 262.487` | `275.952, 276.534, 276.644` |
| Intra 360p | `158.060, 157.876, 158.080` | `158.628, 158.640, 158.657` |
| Intra 720p | `37.678, 37.674, 37.674` | `42.331, 42.214, 42.232` |
| Intra 1080p | `16.148, 16.125, 16.125` | `17.935, 17.941, 17.943` |

Every matched normal-I/P provider-profile record is equal between B4 and B5,
including allocation, H2D, D2H, D2D, synchronization, context-policy, and CUDA
graph fields. Payload counts and peak GPU memory are also equal. Peak GPU memory
remains 2,244 MiB.

## Remaining decision

The worst B5 per-profile throughput change versus B4 is -0.109% for 360p I/P
encode. No numeric acceptance limit is approved, so the measurements cannot
close B5 without that decision.
